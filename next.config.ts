import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Set turbopack.root here if Next infers the wrong workspace root (e.g. multiple lockfiles). */
};

export default nextConfig;
